import 'package:flutter/material.dart';

class SurveySetHowToScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Survey Set Details"),
    );
  }
}